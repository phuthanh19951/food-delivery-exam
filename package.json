{
  "name": "food-delivery-exam",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node app.js",
    "db:model:generate": "sequelize model:generate ",
    "db:migration:generate": "sequelize migration:generate --name ",
    "db:migrate": "sequelize db:migrate --config=false",
    "db:rollback": "sequelize db:migrate:undo"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "~4.16.1",
    "pg": "^8.5.1",
    "pg-hstore": "^2.3.3",
    "sequelize": "^6.5.0"
  }
}
